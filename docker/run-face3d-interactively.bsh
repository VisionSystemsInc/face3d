#!/usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
echo "DIR=${DIR}"
if [ -z "$1"  ]; then
  INSTANCE_NAME=face3d_run
else
  INSTANCE_NAME=$1
fi
echo ${INSTANCE_NAME}
docker-compose -f ${DIR}/docker-compose.yml run --rm  --service-ports --name ${INSTANCE_NAME} \
                   -e user_id=$(id -u $(whoami)) \
                   -e USER=face3d_user \
                   -e DISPLAY=${DISPLAY} face3d
